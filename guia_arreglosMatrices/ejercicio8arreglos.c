/*
Dadas 
#define MAX 1000
typedef int vector_t[MAX];

Desarrollar una función que reciba como parámetros
Un vector tipo vector_t ya cargado y su máximo lógico
Que devuelva:
-Un valor de verdad que informe si el vector está ordenado ascendentemente
-la menor diferencia entre elementos contiguos
Recorriendo una vez el vector
*/

#include<stdio.h>
#define MAX 1000
typedef int vector_t[MAX];

void funcionEjercicio(vector_t vector, int *ml) {
    
}
