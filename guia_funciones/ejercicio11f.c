/*
Escribir una función que dado un código numérico de m dígitos retorne un sub-código consistente en una cantidad 0<n<m de dígitos a partir del k-ésimo lugar al contar del dígito menos significativo.
Los parámetros de la función deben ser código, n y k
En el caso de que los parámetros no sean consistentes y la extracción no puede realizarse la función debe retornar -1
Si el sub-codigo contiene ceros a la izquierda, serán ignorados en el valor entregado como resultado
*/
#include<stdio.h>
void funcionSubCodigo(int codigo, int n, int k) { 
    //No mamen we
}
